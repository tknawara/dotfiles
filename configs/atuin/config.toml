# atuin configuration (managed by gripsack — edit here, `grip apply`)
# (atuin has no [settings] table — keys are top-level; the linter
# caught this: the theme selection below silently did nothing before)

[theme]
name = "catppuccin-mocha-mauve"
